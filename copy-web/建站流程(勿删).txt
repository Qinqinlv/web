第一步，先创建好对应的文件夹
第二步，将需要的静态资源用bower下载
·····

我已经完成了该网站的开发，这个根目录中的文件比较混乱。
比如我的所有路由模块都集中在scripts中。
每一个路由模块的js和img都是从scripts中的文件夹中引入。这很不利于使用
gulp构建工具来构建这些文件。

所以，在下一个根目录done-web中，我会将所有的文件分门别类。
scripts中就只装js文件。
img图片资源全部放入public中。

